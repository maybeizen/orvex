{
  "name": "@orvex/probe",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "go run ./cmd/probe",
    "build": "go build -trimpath -ldflags=\"-s -w -X main.version=0.0.0\" -o dist/orvex-probe ./cmd/probe",
    "lint": "go vet ./...",
    "typecheck": "go test ./... -count=0",
    "test": "go test ./...",
    "clean": "rm -rf dist"
  }
}
